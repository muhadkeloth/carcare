To build time removel
In settings to connect mobile 
* settings->network&internet->
  wi-fi->keralavision->configure firewall and security settings->
* public network->turn on.
or 
* control panel->system and security->windows defender firewall->
* turn windows defender firewall on or off.

git add .
git commit -m "Add OTP input fields with auto-focus and auto-submit functionality"
git push origin main
